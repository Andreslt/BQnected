<%= form_for(@comentario) do |f| %>
  <% if @comentario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
      <% @comentario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comentario %><br />
    <%= f.text_area :comentario %>
  </div>
  <div class="field">
    <%= f.label :evento_id %><br />
    <%= f.select :evento_id, options_for_select(@eventos) %>
  </div>
  <div class="field">
    <%= f.label :asistencia %><br />
    <%= f.check_box :asistencia %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
