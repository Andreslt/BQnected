<%= form_for(@alert) do |f| %>
  <% if @alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alert.errors.count, "error") %> prohibited this alert from being saved:</h2>

      <ul>
      <% @alert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contenido %><br />
    <%= f.text_area :contenido %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.select :tipo, options_for_select(@tiposAlertas) %>
  </div>
  <div class="field">
    <%= f.label :barrio_id %><br />
    <%= f.select :barrio_id, options_for_select(@barrios) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
